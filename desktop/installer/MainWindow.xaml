<Window x:Class="Installer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kino Setup" Height="380" Width="340"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="True" ResizeMode="NoResize"
        Icon="Assets/icon.ico" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Window.Resources>
        <Storyboard x:Key="ProgressAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="ProgressTranslate" Storyboard.TargetProperty="X" From="-90" To="210" Duration="0:0:1.6" />
        </Storyboard>
    </Window.Resources>
    <Border Background="#101117" BorderBrush="#2A2D3A" BorderThickness="1" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image Source="Assets/icon.ico" Width="92" Height="92" HorizontalAlignment="Center" />
                <TextBlock Text="Kino" Foreground="#F8FAFC" FontSize="22" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Margin="0,18,0,0" />
                <TextBlock x:Name="StatusText" Text="Starting..." Foreground="#A9B0C2" FontSize="13"
                           HorizontalAlignment="Center" Margin="0,8,0,0" />
                <Border x:Name="ProgressContainer" Width="210" Height="6" Background="#252936"
                        CornerRadius="3" ClipToBounds="True" Margin="0,24,0,0">
                    <Rectangle Width="90" Height="6" HorizontalAlignment="Left" RadiusX="3" RadiusY="3" Fill="#7C5CFF">
                        <Rectangle.RenderTransform>
                            <TranslateTransform x:Name="ProgressTranslate" X="-90" />
                        </Rectangle.RenderTransform>
                    </Rectangle>
                </Border>
                <TextBlock x:Name="DetailText" Text="" Foreground="#7D8497" FontSize="12" Width="240"
                           TextAlignment="Center" TextWrapping="Wrap" Visibility="Collapsed" Margin="0,16,0,0" />
            </StackPanel>
            <StackPanel x:Name="ActionButtonsPanel" Grid.Row="1" Orientation="Horizontal"
                        HorizontalAlignment="Center" Margin="0,0,0,24" Visibility="Collapsed">
                <Button x:Name="CancelButton" Content="Cancel" Width="96" Height="34" Margin="0,0,10,0"
                        Background="#242833" Foreground="#F8FAFC" BorderBrush="#3B4252" Click="CancelButton_Click" />
                <Button x:Name="ConfirmButton" Content="Uninstall" Width="96" Height="34"
                        Background="#D9435F" Foreground="White" BorderBrush="#D9435F" Click="ConfirmButton_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
